# Backend base URL the app talks to. On a phone this must be the dev machine's
# LAN IP and port, never localhost (that resolves to the phone itself).
# Copy this file to .env and adjust for your network. The EXPO_PUBLIC_ prefix
# makes Expo expose it to the app at build time.
EXPO_PUBLIC_API_BASE_URL=http://192.168.2.18:8080
