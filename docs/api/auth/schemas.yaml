RegisterRequest:
  type: object
  required:
    - full_name
    - email
    - phone
    - password
  properties:
    full_name:
      type: string
    email:
      type: string
      format: email
    phone:
      type: string
    password:
      type: string
      format: password
LoginRequest:
  type: object
  required:
    - email
    - password
  properties:
    email:
      type: string
      format: email
    password:
      type: string
      format: password
VerifyOTPRequest:
  type: object
  required:
    - userId
    - otpCode
  properties:
    userId:
      type: string
    otpCode:
      type: string
