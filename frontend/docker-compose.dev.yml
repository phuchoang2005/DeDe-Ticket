version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "${FRONTEND_PORT_DEV}:5173"
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL}
