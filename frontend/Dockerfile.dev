FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173
# Bind to 0.0.0.0 so the Next dev server is reachable from the host.
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]
